const tipBtn = document.querySelectorAll('.tip-btn');
const tipbox = document.querySelectorAll('.radio-group');

// tipBtn.forEach(btn=>{
//     btn.addEventListener('click',(e)=>{
//         console.log(e.target.value)
//     })
// })
 